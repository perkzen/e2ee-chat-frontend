import React, { FC } from 'react';
import { Box } from '@mui/material';
import { ChatHeader } from '../../ui';

const Chat: FC = () => {
  return (
    <Box>
      <ChatHeader />
    </Box>
  );
};

export default Chat;
